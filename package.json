{
  "name": "monorepo",
  "private": true,
  "version": "0.1.0",
  "devDependencies": {
    "lerna": "^4.0.0",
    "prettier": "^2.3.2"
  },
  "scripts": {
    "bootstrap": "lerna clean -y && lerna bootstrap",
    "build": "lerna run build --parallel --since"
  }
}
